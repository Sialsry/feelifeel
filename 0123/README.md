# DOM, 정규식

## 가위바위보
1. 플레이어, 컴퓨터
> 플레이어 변수, 컴퓨터 변수
2. 컴퓨터는 랜덤한 값으로 가위바위보
3. 플레이어는 버튼을 눌러서 가위 바위 보
> 요소의 onclick 클릭 이벤트로 가위 바위 보 세가지 버튼 중 클릭한 버튼의 내용을 할당
4. 승패를 결정해서 화면에 출력
> 조건문을 통해 승패 결과 값을 구하고 요소의 내용을 포함시켜 

## 정규 표현식
> 문자열을 검색하거나 원하는 문자열을 찾을 때 혹은 규칙에 맞는 문자열인지 확인할 때 사용하는 패턴
> 한마디로 문자의 패턴을 검색하기 위해 사용하는 문자
> 자바스크립트의 내장 객체인 RegExp라는 객체를 제공해준다.
> 예: 회원가입 email 형식으로 작성했는지, 핸드폰 번호 형식인지
> 비밀번호에서 특수문자를 포함했는지 

```js
test(); // 정규표현식에 맞는 문자인지 확인하는 메서드


let reg1 = /a/; // 첫번째 정규식 표현방법 

let reg2 = new RegExp("a"); // 두번째 정규식 표현방법

// 위처럼 이렇게 정규표현식을 만드는 이유는 규칙을 가진 문자열을 찾기 위해서 

let reg3 = /[3,6,9,10,11]/ // [] 내부의 문자열 중에 하나

let reg4 = /[0~9]/ // 0부터9까지 중에 포함되어 있는지

// 문자열 검색
let str = "hello javascript..."
let reg5 = str.search(/javascript/); // search: 문자열 안에 포함된 메서드. 
console.log(reg5);

// 문자열을 찾아서 다른 문자열로 변환
let reg6 = str.replace(/javascript/, "css")





// 정규 표현식의 플래그
// 문자열 규칙을 비교할때 옵션을 설정할 수 있다. 
// i: 대소문자를 구별하지 않고 비교
// g: 전체 문자열의 정규식과 비교 
// m: 줄이 다른 문자열의 정규표현식 비교

let str2 = "program javascript \n and Html css"

let reg9 = /html/i;
console.log(reg9.metch(str2)) // 반환값이 배열의 형태


// 정규식의 메타 문자
// 메타 문자는 숫자는 혹은 알파벳만 내용을정의할 수 있다.
// 메타문자로 표현한다.

/*
정규식은 솔직히 너무 너무 너무 암기영역중에 암기영역입니다 필수로 전체를 암기하기에는너무많아요공식문서가면무수하게많습니다css크기만큼많아요얘가 표현식의 형태정도만비밀번호입력이나어려워하실수있는데입력이나 이메일형식들을없긴할거에요실무하실때

^^웃으며 삽시다
^문자 : 정규 표현식에서는 시작의 문자가 맞는지 확인한다. 
문자$ : 정규 표현식에서는 끝나는 문자가 맞는지 확인한다람쥐.
\w : 모든 문자를 찾는다 속하는 모든 문자열
\W : 알파벳 대소문자, 숫자 _문자를 제외한 모든 문자를 찾는다.
\d :숫자를 찾는다
\D ,숫잘를 제외하고 문자를 찾는다
\s 공백문자를 찾는다
\s 공백문자를 제외하고 찾는다. 
*/

let str4 = "Type program is javascript" 
let reg12 = /^T/ig;
console.log(str4.match(reg12))

```