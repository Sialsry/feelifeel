# 논리 연산자, 스코프, 조건문, 반복문, 객체의 특징과 값의 호출

## 논리 연산자 

> 반환하는 데이터 타입은 bool
> 두가지의 값을 확인해서 true false 반환한다.

//


## 논리연산자의 종류
- || (OR) : 둘 중에 하나라도 참이면 참, 둘 다 거짓이면 거짓

```js
[false || true] === true



```

- && (AND) : 둘 다 참이어야 참, 하나라도 거짓이면 거짓

```js
[true && true] === true


```


## 스코프 
```js

> 전역 스코프
// 전역스코프는 프로그램을 종료하는 시점까지 메모리에 유지된다. 전역스코프의 변수는 어느곳에서든 참조 가능


> 지역 스코프
// 지역 스코프 간에는 참조 불가능

```




### 조건문 
```js

const RUN = "RUN";
const STATE = "STATE";
const WORK = "RUN";

let myState = "RUN";
switch(mtState) {
    case RUN : console.log()
    break;
}

```


### 반복문
```js
//while
[while 예약어] (조건식) {
    코드 영역
}
while (true) {
    if count

}


```

### 객체의 특징과 값의 흐름
> 객체의 4가지 특징


- 추상화: 
- 상속
- 다형성
- 캡슐화

## 객체의 표현 방식 
1. 객체 리터럴
2. function 생성방식
3. class 생성 방식

{} : 객체 리터럴 구문

### 데이터의 타입 참조 타입
> 원시타입과 다르다.
> array, object, function, Map, set ...

> 주소를 참조하는 주소

### 객체의 문법

```js
//리터럴 방식
const user = {
    name : "soon",
    age : 20, 
    city : "서울"
}
// 하나의 사물을 표현할 때 여러가지의 데이터를 하나의 객체단위로 사용하기 위해서

// 생성자 방식은 function class 배울 때 new

// 실제로 존재하는 사물이나 개념을 가지고 구상해서 만들 때 사물의 단위를 표현할 때 객체를 만듦


const computer = {
    name : "ASUS2",
    model : 14,
    color : "black"
    sell : false,
    price : 100
}
// 객체 값의 호출
// .표기법 ---------------------------------------------------------------------------------------------------------------------
computer.name === "ASUS2"




/// 대괄호 표기법
computer.name === computer["name"]


```


### 실습 과제
> 가위 바위 보 게임

- 요구사항 1 : 플레이어가 가위/ 바위/ 보 입력
                    가위 0 바위 1 보 2
- 요구사항 2 : 컴퓨터는 랜덤하게 가위바위보 선택
- 요구사항 3 : 무승부도 있습니다
- 요구사항 4 : 입력받은 값이 잘못된 경우 다시 입력받으세요 계속
- 요구사항 5 : 논리연산자 ||, && 둘중 하나 활용하세요


















