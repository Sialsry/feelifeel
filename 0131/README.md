# 로컬 스토리지
> 브라우저의 설치 경로에 로컬 스토리지 데이터가 저장된다. 
> 브라우저를 꺼도 데이터가 남아있다.
> 민감한 데이터는 저장하면 안된다. 

1. 브라우저에 저장되는 데이터 로컬에 저장된다. 
2. 용량 5MB까지 저장 가능
3. 보안 문제 javascript에서 접근이 누구나 가능한 데이터이기 때문에 민감한 정보는 저장하면 안된다.
4. 데이터의 타입은 문자열로 저장된다. 



## 로컬 스토리지 객체 메서드
```js
window.localstorage === localstorage // 전역 객체 생략 가능
localstorage.setItem("key","value"); // 저장할 데이터의 키와 값 

// getItem : 데이터를 불러오는 메서드. 반환값의 데이터 타입은 문자열
let data = localstorage.getItem("key");

// removeItem : 데이터 삭제 메서드
localstarage.removeItem("name")

// length : 저장된 데이터 갯수를 확인
console.log(localStorage.length)

// 값을 저장할때 인덱스 번호가 있다. 
// 로컬스토리지에 저장한 순서대로 키를 조회하는 메서드 
console.log(localStrage.key(0));




```

> 게시판을 만든것에서 ud직접 구현하기
> tip 로컬스토리지의 데이터가 진짜 데이터 실제 데이터를 받아서 처리하는 영역이고 데이터를 지웠다거나 수정했다는것은 저장소의 값이 변경되거나 사라진경우 데이터를 수정하거나 삭제한 이후 렌더링을 해주면 된다. 
1. 수정할때 json 안의 값을 가지고와서 배열의 형태로 바꾼 뒤 배열에서 값을 제거하고 다시 json 문자열로 바꿔서 값을 저장 
2. 삭제도 배열에서 제거할 값을 제거하고 json문자열로 바꿔서 저장하면 끝 
3. 로컬스토리지의 데이터가 삭제된 경우 index 정렬 한번 